numpy
pandas
pillow
opencv-python
pycocotools
torchmetrics
segmentation-models-pytorch
timm
matplotlib
tqdm
kagglehub
python-dotenv

# torch/torchvision: install separately with the CUDA build matching your GPU driver, e.g.:
#   pip install torch==2.13.0+cu130 torchvision==0.28.0+cu130 --index-url https://download.pytorch.org/whl/cu130
# A plain `pip install torch torchvision` from PyPI pulls CPU-only wheels on Windows.
torch
torchvision
